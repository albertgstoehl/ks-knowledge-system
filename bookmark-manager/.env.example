# Optional: Jina AI API key for higher rate limits
JINA_API_KEY=

# Claude Code OAuth token for LLM summarization (uses your subscription)
# Generate with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Database
# Note: Use 4 slashes (sqlite:////) for absolute paths in Docker
DATABASE_URL=sqlite:////app/data/bookmarks.db
